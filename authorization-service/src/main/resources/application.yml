spring:
  application:
    name: authorization-service
  datasource:
    url: jdbc://postgres://localhost:5432/spring_microservices
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver

server:
  port: ${PORT:0}

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
